/* DeutschWeg X12 — Product Experience Rebuild */
(() => {
  'use strict';

  const LEVELS=['A1','A2','B1','B2','C1','C2'];
  const SKILLS=['Lesen','Hören','Schreiben','Sprechen','Grammatik','Wortschatz'];
  const LESSONS={
    A1:{title:['اولین گفت‌وگوی واقعی','Dein erstes echtes Gespräch'],goal:['معرفی خود و پرسیدن نام','Sich vorstellen und nach dem Namen fragen'],phrase:'Hallo, ich heiße Mina. Wie heißt du?',fa:'سلام، من مینا هستم. اسم تو چیست؟',vocab:['heißen','Hallo','wie'],explain:['برای معرفی خود از «Ich heiße …» استفاده می‌کنیم.','Für die Vorstellung nutzt man „Ich heiße …“.'],q:'کدام جمله برای معرفی خود درست است؟',options:['Ich heiße Mina.','Ich heißen Mina.','Ich heißt Mina.'],answer:0,skill:'Sprechen'},
    A2:{title:['قرار و برنامه روزانه','Termine und Alltag'],goal:['هماهنگ‌کردن یک قرار ساده','Einen einfachen Termin vereinbaren'],phrase:'Können wir uns morgen um zehn Uhr treffen?',fa:'می‌توانیم فردا ساعت ده همدیگر را ببینیم؟',vocab:['sich treffen','morgen','um zehn Uhr'],explain:['برای پیشنهاد مودبانه می‌توان از «Können wir …?» استفاده کرد.','Für einen höflichen Vorschlag eignet sich „Können wir …?“.'],q:'معنی «einen Termin vereinbaren» چیست؟',options:['تنظیم یک قرار','لغو یک سفر','خرید بلیت'],answer:0,skill:'Hören'},
    B1:{title:['بیان نظر و دلیل','Meinung und Begründung'],goal:['یک نظر را با دلیل روشن بیان کنی','Eine Meinung klar begründen'],phrase:'Meiner Meinung nach ist das sinnvoll, weil es Zeit spart.',fa:'به نظر من این کار منطقی است، چون در زمان صرفه‌جویی می‌کند.',vocab:['meiner Meinung nach','sinnvoll','Zeit sparen'],explain:['در جمله وابسته با weil، فعل صرف‌شده در پایان می‌آید.','Im weil-Satz steht das konjugierte Verb am Ende.'],q:'کدام جمله درست است؟',options:['weil es Zeit spart','weil es spart Zeit','weil spart es Zeit'],answer:0,skill:'Grammatik'},
    B2:{title:['استدلال حرفه‌ای','Professionell argumentieren'],goal:['مزایا و محدودیت‌ها را متعادل بیان کنی','Vorteile und Grenzen ausgewogen darstellen'],phrase:'Einerseits bietet die Lösung klare Vorteile, andererseits entstehen neue Risiken.',fa:'از یک طرف راه‌حل مزایای روشنی دارد و از طرف دیگر ریسک‌های جدیدی ایجاد می‌شود.',vocab:['einerseits','andererseits','Risiken entstehen'],explain:['ساختار «einerseits … andererseits …» برای استدلال متوازن کاربردی است.','„Einerseits … andererseits …“ strukturiert eine ausgewogene Argumentation.'],q:'کدام ساختار تضاد متوازن می‌سازد؟',options:['einerseits … andererseits','weil … deshalb','entweder … weder'],answer:0,skill:'Schreiben'},
    C1:{title:['بیان دقیق و آکادمیک','Präziser akademischer Ausdruck'],goal:['ادعا را با احتیاط و دقت صورت‌بندی کنی','Aussagen differenziert und vorsichtig formulieren'],phrase:'Die vorliegenden Ergebnisse deuten darauf hin, dass weitere Untersuchungen erforderlich sind.',fa:'نتایج موجود نشان می‌دهد که بررسی‌های بیشتری ضروری است.',vocab:['darauf hindeuten','vorliegend','erforderlich'],explain:['«darauf hindeuten, dass …» برای بیان نتیجه بدون قطعیت بیش از حد مناسب است.','„darauf hindeuten, dass …“ formuliert Schlussfolgerungen ohne übermäßige Sicherheit.'],q:'کدام عبارت برای نتیجه‌گیری محتاطانه رسمی‌تر است؟',options:['Die Ergebnisse deuten darauf hin, dass …','Das ist hundertprozentig so.','Ich glaube halt, dass …'],answer:0,skill:'Schreiben'},
    C2:{title:['ظرافت سبکی و معنایی','Stilistische und semantische Nuancen'],goal:['میان لحن قطعی، محتاطانه و انتقادی تمایز بگذاری','Zwischen sicherem, vorsichtigem und kritischem Ton unterscheiden'],phrase:'Diese Interpretation ist zwar nachvollziehbar, greift jedoch in wesentlichen Punkten zu kurz.',fa:'این تفسیر هرچند قابل‌درک است، اما در نکات اساسی ناکافی است.',vocab:['nachvollziehbar','zu kurz greifen','wesentlich'],explain:['«zwar … jedoch …» امکان پذیرش محدود و سپس نقد دقیق را می‌دهد.','„Zwar … jedoch …“ erlaubt eine begrenzte Zustimmung mit anschließender präziser Kritik.'],q:'«zu kurz greifen» در این بافت یعنی چه؟',options:['ناکافی بودن تحلیل','کوتاه نوشتن','سریع نتیجه گرفتن فیزیکی'],answer:0,skill:'Lesen'}
  };

  let session=null;
  const legacy={
    renderHome:window.renderHome,
    renderPractice:window.renderPractice,
    renderReport:window.renderReport
  };

  function x12(){
    state.learning=state.learning||{};
    state.learning.x12=state.learning.x12||{daily:{},sessions:[],mastery:{},lastLesson:null,achievements:[]};
    state.learning.x12.daily=state.learning.x12.daily||{};
    state.learning.x12.sessions=state.learning.x12.sessions||[];
    state.learning.x12.mastery=state.learning.x12.mastery||{};
    state.learning.x12.achievements=state.learning.x12.achievements||[];
    return state.learning.x12;
  }
  function todayKey(){return new Date().toISOString().slice(0,10)}
  function daily(){let x=x12();x.daily[todayKey()]=x.daily[todayKey()]||{lesson:false,speak:false,review:false};return x.daily[todayKey()]}
  function currentLevel(){let l=state.profile.currentLevel;return LEVELS.includes(l)?l:(LEVELS.includes(state.profile.targetLevel)?state.profile.targetLevel:'A1')}
  function targetLevel(){return LEVELS.includes(state.profile.targetLevel)?state.profile.targetLevel:currentLevel()}
  function lessonFor(){return LESSONS[targetLevel()]||LESSONS[currentLevel()]||LESSONS.A1}
  function mastery(skill){let local=Number(x12().mastery[skill]||0);try{let m=skillMastery(targetLevel())[skill];if(m&&m.a)return Math.max(local,Number(m.pct||0))}catch(e){}return local}
  function weakestSkill(){let arr=SKILLS.map(s=>[s,mastery(s)]).sort((a,b)=>a[1]-b[1]);return arr[0][0]}
  function missionPct(){let d=daily();return Math.round(([d.lesson,d.speak,d.review].filter(Boolean).length/3)*100)}
  function cefrPct(){let ti=LEVELS.indexOf(targetLevel()),ci=LEVELS.indexOf(currentLevel());let base=Math.max(0,ci)*16.67,span=Math.max(1,ti-ci+1)*16.67;let skills=SKILLS.reduce((a,s)=>a+mastery(s),0)/SKILLS.length;return Math.max(2,Math.min(99,Math.round(base+(skills/100)*span))) }
  function studyByDay(days=7){let out=[];for(let i=days-1;i>=0;i--){let d=new Date();d.setDate(d.getDate()-i);let key=d.toISOString().slice(0,10),mins=0;(state.studySessions||[]).forEach(s=>{if((s.date||'').slice(0,10)===key)mins+=Number(s.minutes||0)});out.push({key,mins,label:new Intl.DateTimeFormat(state.lang==='de'?'de-DE':'fa-IR',{weekday:'short'}).format(d)})}return out}
  function totalMinutes(){return (state.studySessions||[]).reduce((a,s)=>a+Number(s.minutes||0),0)}
  function completedLessonCount(){return x12().sessions.length}
  function streak(){try{return studySummary().streak||0}catch(e){return 0}}
  function nextAction(){
    if(state.profile.placementNeeded)return {kind:'placement',title:t('اول سطح فعلی را مشخص کن','Bestimme zuerst dein Niveau'),sub:t('یک ارزیابی کوتاه، مسیر را دقیق‌تر می‌کند.','Eine kurze Einstufung macht deinen Lernweg präziser.'),cta:t('شروع تعیین سطح','Einstufung starten')};
    let d=daily(),w=weakestSkill();
    if(!d.lesson)return {kind:'lesson',title:t('درس تطبیقی امروز','Deine adaptive Lektion'),sub:t(`تمرکز امروز: ${moduleName(w)}`,`Heutiger Fokus: ${moduleName(w)}`),cta:t('شروع درس','Lektion starten')};
    if(!d.speak)return {kind:'speak',title:t('حالا زبان را فعال کن','Jetzt aktiv sprechen'),sub:t('یک تمرین گفتاری کوتاه برای تثبیت جلسه.','Kurze Sprechübung zur Festigung.'),cta:t('شروع گفتار','Sprechen starten')};
    if(dueCardsCount()>0)return {kind:'review',title:t('مرور هوشمند','Intelligente Wiederholung'),sub:t(`${dueCardsCount()} کارت آماده مرور است.`,`${dueCardsCount()} Karten sind fällig.`),cta:t('شروع مرور','Wiederholen')};
    return {kind:'lesson',title:t('یک قدم جلوتر','Ein Schritt weiter'),sub:t('جلسه بعدی را برای تثبیت مسیر شروع کن.','Starte die nächste Einheit und festige deinen Pfad.'),cta:t('ادامه یادگیری','Weiterlernen')};
  }
  function doNext(kind){if(kind==='placement')return openResource('goethe-test');if(kind==='review'){state.ui.practiceTab='cards';state.ui.x12Mode='tools';save();go('practice');return}startX12Lesson(kind==='speak'?4:0)}
  window.x12DoNext=doNext;

  function missionHTML(){let d=daily(),pct=missionPct();let rows=[['lesson','◉',t('یک درس کامل','Eine Lektion abschließen'),d.lesson],['speak','●',t('یک تمرین گفتاری','Eine Sprechübung'),d.speak],['review','◆',t('مرور و جمع‌بندی','Wiederholen & festigen'),d.review]];return `<div class="x12-mission"><div class="x12-ring" style="--mission:${pct}%"><b>${pct}%</b></div><div class="x12-checklist">${rows.map(r=>`<div class="x12-check ${r[3]?'done':''}"><i>${r[3]?'✓':r[1]}</i><b>${r[2]}</b><span>${r[3]?t('انجام شد','Erledigt'):t('باز','Offen')}</span></div>`).join('')}</div></div>`}
  function skillHTML(){return SKILLS.map(s=>{let v=mastery(s),ico=(MODULES.find(m=>m.id===s)||{}).icon||'•';return `<button class="x12-skill" onclick="state.ui.x12Mode='tools';state.ui.practiceTab='exercise';save();renderPractice()"><strong><span>${ico} ${moduleName(s)}</span><em>${v}%</em></strong><small>${v<40?t('نیاز به تمرکز','Fokus nötig'):v<70?t('در حال رشد','Im Aufbau'):t('قوی','Stark')}</small><div class="x12-mini"><i style="width:${v}%"></i></div></button>`}).join('')}
  function barsHTML(){let days=studyByDay(),max=Math.max(20,...days.map(d=>d.mins));return `<div class="x12-week-bars">${days.map(d=>`<div class="x12-daybar"><i style="height:${Math.max(5,Math.round(d.mins/max*100))}%" title="${d.mins} min"></i><span>${d.label}</span></div>`).join('')}</div>`}

  window.renderHome=function(){
    let action=nextAction(),p=cefrPct(),l=lessonFor(),cur=state.profile.currentLevel==='UNKNOWN'?'?':currentLevel(),tar=targetLevel(),sum=studySummary(),who=state.profile.name?esc(state.profile.name):'',hour=new Date().getHours(),hello=hour<12?t('صبح بخیر','Guten Morgen'):hour<18?t('روز بخیر','Guten Tag'):t('عصر بخیر','Guten Abend');
    document.getElementById('view-home').innerHTML=`<div class="x12-shell">
      <section class="x12-hero"><div><span class="x12-kicker">DeutschWeg X12 · Next Best Action</span><h2>${hello}${who?'، '+who:''}</h2><p>${t('برنامه امروز از روی سطح، ضعف‌های ثبت‌شده و فعالیت‌های اخیرت ساخته می‌شود. یک قدم واضح، بدون شلوغی ابزارها.','Dein heutiger Weg basiert auf Niveau, erkannten Schwächen und deiner letzten Aktivität. Ein klarer nächster Schritt statt Tool-Chaos.')}</p></div><div class="x12-next-card"><small>${t('قدم بعدی پیشنهادی','Empfohlener nächster Schritt')}</small><b>${action.title}</b><div class="x12-level-track"><i style="width:${p}%"></i></div><div class="x12-level-meta"><span>${cur} → ${tar}</span><span>${p}% CEFR Journey</span></div><button class="x12-primary-action" onclick="x12DoNext('${action.kind}')"><span><b>${action.cta}</b><small>${action.sub}</small></span><em>→</em></button></div></section>
      <div class="x12-grid"><main class="x12-stack">
        <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Daily Mission</span><h3>${t('ماموریت امروز','Deine Mission heute')}</h3></div><button class="x12-link" onclick="startX12Lesson(0)">${t('شروع','Start')} →</button></div>${missionHTML()}</section>
        <section class="x12-panel soft"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Adaptive lesson</span><h3>${pair(l.title[0],l.title[1])}</h3><p>${pair(l.goal[0],l.goal[1])}</p></div><span class="chip">${targetLevel()}</span></div><div class="x12-phrase">${l.phrase}<small>${l.fa}</small></div><button class="btn btn-primary wide" style="margin-top:12px" onclick="startX12Lesson(0)">▶ ${t('ورود به جلسه کامل','Komplette Sitzung starten')}</button></section>
        <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Skill Pulse</span><h3>${t('مهارت‌هایی که موتور تطبیقی می‌بیند','Was die adaptive Engine sieht')}</h3></div></div><div class="x12-skill-grid">${skillHTML()}</div></section>
      </main><aside class="x12-stack">
        <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Consistency</span><h3>${t('هفت روز اخیر','Letzte 7 Tage')}</h3></div><b> ${num(streak())}</b></div>${barsHTML()}<div class="small muted" style="margin-top:8px">${fmtMin(sum.week||0)} ${t('این هفته','diese Woche')}</div></section>
        <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Quick tools</span><h3>${t('ابزارهای سریع','Schnellzugriff')}</h3></div></div><div class="x12-quick"><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='vocab';save();go('practice')"><i>V</i><b>${t('واژگان','Wortschatz')}</b></button><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='pron';save();go('practice')"><i>S</i><b>${t('تلفظ','Aussprache')}</b></button><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='podcast';save();go('practice')"><i>H</i><b>${t('پادکست','Podcast')}</b></button><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='cards';save();go('practice')"><i>K</i><b>${t('مرور','Wiederholen')}</b></button></div></section>
        <section class="x12-panel accent"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Milestone</span><h3>${completedLessonCount()?t(`${completedLessonCount()} جلسه کامل شده`,`${completedLessonCount()} Sitzungen abgeschlossen`):t('اولین Milestone را بساز','Erreiche deinen ersten Meilenstein')}</h3><p>${t('پیشرفت واقعی از جلسه‌های کامل، نه تعداد کلیک‌ها، محاسبه می‌شود.','Echter Fortschritt basiert auf abgeschlossenen Sitzungen, nicht auf Klicks.')}</p></div></div></section>
      </aside></div></div>`;
  };

  function journeyHTML(){let cur=LEVELS.indexOf(currentLevel()),tar=LEVELS.indexOf(targetLevel());return `<div class="x12-journey">${LEVELS.map((l,i)=>`<div class="x12-node ${i<cur?'done':''} ${i===tar?'active':''}"><i>${l}</i><span>${i===tar?t('هدف','Ziel'):i===cur?t('فعلی','Jetzt'):''}</span></div>`).join('')}</div>`}
  function x12Learn(){let l=lessonFor(),w=weakestSkill();return `<div class="x12-shell"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Learning Journey</span><h2 style="font-size:28px;margin:3px 0">${t('مسیر یادگیری تو','Dein Lernweg')}</h2><p>${t('از سطح فعلی تا هدف، با جلسه‌های مرحله‌ای و بازخورد واقعی.','Vom aktuellen Niveau bis zum Ziel – mit geführten Sitzungen und echtem Feedback.')}</p></div><button class="btn btn-secondary" onclick="state.ui.x12Mode='tools';save();renderPractice()">${t('کتابخانه ابزارها','Tool-Bibliothek')} ↗</button></div>
    <section class="x12-panel">${journeyHTML()}</section>
    <section class="x12-panel soft"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Recommended now</span><h3>${pair(l.title[0],l.title[1])}</h3><p>${t(`ضعیف‌ترین سیگنال فعلی: ${moduleName(w)}`,`Aktuell schwächstes Signal: ${moduleName(w)}`)}</p></div><span class="chip">${targetLevel()} · 12–18 min</span></div><div class="x12-lesson-list"><div class="x12-lesson-row"><i>▶</i><div><b>${pair(l.goal[0],l.goal[1])}</b><small>Warm-up → Learn → Listen → Practice → Speak → Review → Score</small></div><button onclick="startX12Lesson(0)">${t('شروع','Start')}</button></div></div></section>
    <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Mastery</span><h3>${t('تمرکز مهارتی','Skill-Fokus')}</h3></div></div><div class="x12-skill-grid">${skillHTML()}</div></section>
    <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Learning modes</span><h3>${t('وقتی لازم داری، ابزار را باز کن','Tools nur dann, wenn du sie brauchst')}</h3></div></div><div class="x12-quick"><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='exercise';save();renderPractice()"><i>G</i><b>${t('تمرین','Übungen')}</b></button><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='vocab';save();renderPractice()"><i>V</i><b>${t('واژگان','Wortschatz')}</b></button><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='podcast';save();renderPractice()"><i>P</i><b>Podcast</b></button><button onclick="state.ui.x12Mode='tools';state.ui.practiceTab='resources';save();renderPractice()"><i>↗</i><b>${t('منابع','Quellen')}</b></button></div></section></div>`}

  window.renderPractice=function(){
    if(state.ui.x12Mode==='tools'){
      legacy.renderPractice();
      let el=document.getElementById('view-practice');
      el.insertAdjacentHTML('afterbegin',`<div class="x12-panel" style="margin-bottom:12px;display:flex;align-items:center;justify-content:space-between;gap:10px"><div><b>${t('کتابخانه ابزارها','Tool-Bibliothek')}</b><div class="small muted">${t('ابزارهای تخصصی؛ مسیر اصلی یادگیری در Journey قرار دارد.','Spezialwerkzeuge; dein Hauptlernweg bleibt in Journey.')}</div></div><button class="btn btn-secondary" onclick="state.ui.x12Mode='journey';save();renderPractice()">← ${t('بازگشت به مسیر','Zurück zum Lernweg')}</button></div>`);
      return;
    }
    document.getElementById('view-practice').innerHTML=x12Learn();
  };

  window.renderReport=function(){
    let sum=studySummary(),p=cefrPct(),days=studyByDay(),heat=[];for(let i=27;i>=0;i--){let d=new Date();d.setDate(d.getDate()-i);let key=d.toISOString().slice(0,10),mins=0;(state.studySessions||[]).forEach(s=>{if((s.date||'').slice(0,10)===key)mins+=Number(s.minutes||0)});heat.push(mins)}
    let max=Math.max(1,...heat),heatHtml=heat.map(v=>`<i class="${v===0?'':v<max*.25?'l1':v<max*.5?'l2':v<max*.8?'l3':'l4'}" title="${v} min"></i>`).join('');
    document.getElementById('view-report').innerHTML=`<div class="x12-shell"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Progress Experience</span><h2 style="font-size:28px;margin:3px 0">${t('پیشرفت واقعی تو','Dein echter Fortschritt')}</h2><p>${t('تمرکز روی تسلط، ثبات و آمادگی سطح؛ نه فقط تعداد فعالیت‌ها.','Fokus auf Beherrschung, Konsistenz und Niveaubereitschaft – nicht nur Aktivitätszahlen.')}</p></div></div>
      <div class="x12-progress-hero"><section class="x12-big-stat"><span>CEFR Journey</span><b>${p}%</b><span>${currentLevel()} → ${targetLevel()}</span></section><div class="x12-kpis"><div class="x12-kpi"><b>${fmtMin(totalMinutes())}</b><span>${t('کل زمان ثبت‌شده','Gesamte Lernzeit')}</span></div><div class="x12-kpi"><b>${completedLessonCount()}</b><span>${t('جلسه کامل','Abgeschlossene Sitzungen')}</span></div><div class="x12-kpi"><b> ${streak()}</b><span>${t('روز پیوسته','Streak')}</span></div><div class="x12-kpi"><b>${missionPct()}%</b><span>${t('ماموریت امروز','Heutige Mission')}</span></div></div></div>
      <section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Mastery map</span><h3>${t('نقشه تسلط مهارتی','Kompetenzprofil')}</h3></div></div><div class="x12-skill-grid">${skillHTML()}</div></section>
      <div class="x12-grid"><section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Consistency</span><h3>${t('ریتم هفتگی','Wochenrhythmus')}</h3></div></div>${barsHTML()}</section><section class="x12-panel"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">28 days</span><h3>${t('نقشه فعالیت','Aktivitätskarte')}</h3></div></div><div class="x12-heat">${heatHtml}</div></section></div>
      <section class="x12-panel accent"><div class="x12-head"><div><span class="x12-kicker" style="color:var(--primary)">Next focus</span><h3>${t(`مهارت بعدی: ${moduleName(weakestSkill())}`,`Nächster Fokus: ${moduleName(weakestSkill())}`)}</h3><p>${t('موتور تطبیقی این مهارت را پایین‌ترین سیگنال ثبت‌شده تشخیص داده است.','Die adaptive Engine erkennt hier aktuell das schwächste Signal.')}</p></div><button class="btn btn-primary" onclick="startX12Lesson(0)">${t('شروع جلسه هدفمند','Gezielte Sitzung starten')}</button></div></section></div>`;
  };

  function ensureOverlay(){let el=document.getElementById('x12LessonOverlay');if(!el){el=document.createElement('div');el.id='x12LessonOverlay';el.className='x12-lesson-overlay';document.body.appendChild(el)}return el}
  function startX12Lesson(step=0){let l=lessonFor();session={id:'x12-'+Date.now(),lesson:l,level:targetLevel(),skill:l.skill||weakestSkill(),step:Math.max(0,Math.min(6,step)),practice:null,speakScore:0,transcript:'',review:false,startedAt:Date.now()};ensureOverlay().classList.add('show');renderStage()}
  window.startX12Lesson=startX12Lesson;
  function closeLesson(){let el=ensureOverlay();el.classList.remove('show');session=null}
  window.closeX12Lesson=closeLesson;
  function stageNames(){return [t('آماده‌سازی','Warm-up'),t('یادگیری','Learn'),t('شنیدن','Listen'),t('تمرین','Practice'),t('صحبت','Speak'),t('مرور','Review'),t('نتیجه','Score')]}
  function renderStage(){if(!session)return;let el=ensureOverlay(),s=session,l=s.lesson,names=stageNames(),pct=Math.round((s.step/(names.length-1))*100);let body='';
    if(s.step===0)body=`<div class="eyebrow">Warm-up · ${s.level}</div><h2>${pair(l.title[0],l.title[1])}</h2><p>${pair(l.goal[0],l.goal[1])}</p><div class="x12-phrase">${l.phrase}<small>${l.fa}</small></div>`;
    if(s.step===1)body=`<div class="eyebrow">Learn</div><h2>${t('یک الگوی کاربردی یاد بگیر','Lerne ein nützliches Muster')}</h2><p>${pair(l.explain[0],l.explain[1])}</p><div class="x12-panel"><b>${t('واژگان کلیدی','Schlüsselwörter')}</b><div class="chiprow" style="margin-top:10px">${l.vocab.map(v=>`<span class="chip">${v}</span>`).join('')}</div></div>`;
    if(s.step===2)body=`<div class="eyebrow">Listen</div><h2>${t('بشنو، مکث کن، تکرار کن','Hören, pausieren, nachsprechen')}</h2><p>${t('ابتدا بدون نگاه‌کردن به ترجمه گوش بده. سپس جمله را یک‌بار با صدای بلند تکرار کن.','Höre zuerst ohne Übersetzung. Sprich den Satz danach einmal laut nach.')}</p><div class="x12-phrase">${l.phrase}<small>${l.fa}</small></div><button class="x12-audio-btn" onclick="x12SpeakPhrase()"></button>`;
    if(s.step===3)body=`<div class="eyebrow">Practice</div><h2>${l.q}</h2><div class="x12-answer-grid">${l.options.map((o,i)=>`<button class="x12-answer ${s.practice===i?(i===l.answer?'correct':'wrong'):''}" onclick="x12ChooseAnswer(${i})">${o}</button>`).join('')}</div>${s.practice!==null?`<p>${s.practice===l.answer?t('درست است. الگو را در جمله بعدی هم نگه دار.','Richtig. Behalte dieses Muster für den nächsten Schritt.'):t('این پاسخ درست نیست؛ الگوی درس را دوباره نگاه کن.','Nicht ganz. Schau dir das Muster noch einmal an.')}</p>`:''}`;
    if(s.step===4)body=`<div class="eyebrow">Speak</div><h2>${t('حالا خودت بگو','Jetzt selbst sprechen')}</h2><p>${t('جمله را با صدای طبیعی بگو. امتیاز مرورگر فقط بازخورد تمرینی است، نه ارزیابی رسمی تلفظ.','Sprich den Satz natürlich. Die Browser-Erkennung ist nur Übungsfeedback, keine offizielle Aussprachebewertung.')}</p><div class="x12-phrase">${l.phrase}</div><div class="x12-speak-box"><button id="x12Mic" class="x12-mic" onclick="x12StartRecognition()">P</button><div class="x12-transcript">${s.transcript||t('برای شروع روی میکروفن بزن','Tippe auf das Mikrofon')}</div>${s.transcript?`<div class="chip" style="margin-top:8px">${t('شباهت تمرینی','Übungsähnlichkeit')}: ${s.speakScore}%</div>`:''}</div>`;
    if(s.step===5)body=`<div class="eyebrow">Review</div><h2>${t('یک بار از حافظه بازیابی کن','Einmal aktiv aus dem Gedächtnis abrufen')}</h2><p>${t('قبل از نگاه‌کردن، معنی و ساختار جمله را در ذهن بازسازی کن. سپس پاسخ را آشکار کن.','Rekonstruiere Bedeutung und Struktur zuerst aus dem Gedächtnis. Zeige danach die Lösung.')}</p><button class="x12-answer" onclick="this.nextElementSibling.hidden=false;this.hidden=true">${t('نمایش پاسخ','Lösung zeigen')}</button><div class="x12-phrase" hidden>${l.phrase}<small>${l.fa}</small></div>`;
    if(s.step===6){let score=scoreSession();body=`<div class="eyebrow">Session complete</div><div class="x12-score" style="--score:${score}%"><b>${score}</b></div><h2 style="text-align:center">${score>=80?t('جلسه قوی بود','Starke Sitzung'):score>=60?t('پیشرفت خوبی بود','Guter Fortschritt'):t('این بخش نیاز به یک مرور دیگر دارد','Dieser Bereich braucht noch eine Wiederholung')}</h2><p style="text-align:center">${t(`${moduleName(s.skill)} · ${s.level} · جلسه در پروفایل پیشرفت ثبت شد.`,`${moduleName(s.skill)} · ${s.level} · Die Sitzung wurde im Fortschrittsprofil gespeichert.`)}</p>`}
    el.innerHTML=`<div class="x12-lesson-shell"><div class="x12-lesson-top"><button class="x12-close" onclick="closeX12Lesson()">×</button><div><div class="x12-step-track"><i style="width:${pct}%"></i></div><div class="x12-step-label">${names[s.step]} · ${s.step+1}/${names.length}</div></div><div></div></div><div></div><main class="x12-stage">${body}</main><footer class="x12-footer"><button class="x12-back" onclick="x12LessonBack()" ${s.step===0?'disabled':''}>←</button><button class="x12-next" onclick="x12LessonNext()">${s.step===6?t('پایان','Fertig'):t('ادامه','Weiter')} →</button></footer></div>`;
  }
  function scoreSession(){let a=session.practice===session.lesson.answer?100:session.practice===null?45:35,b=session.speakScore||50;return Math.round(a*.55+b*.45)}
  window.x12ChooseAnswer=i=>{if(!session)return;session.practice=i;renderStage()};
  window.x12SpeakPhrase=()=>{if(!session||!('speechSynthesis'in window))return;let u=new SpeechSynthesisUtterance(session.lesson.phrase);u.lang='de-DE';u.rate=.9;speechSynthesis.cancel();speechSynthesis.speak(u)};
  function similarity(a,b){let clean=x=>x.toLowerCase().replace(/[^a-zäöüß\s]/gi,'').split(/\s+/).filter(Boolean),A=clean(a),B=clean(b);if(!B.length)return 0;let hit=B.filter(w=>A.includes(w)).length;return Math.round(hit/B.length*100)}
  window.x12StartRecognition=()=>{if(!session)return;let R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R){session.transcript=t('تشخیص گفتار در این مرورگر در دسترس نیست. جمله را دستی تکرار کن.','Spracherkennung ist in diesem Browser nicht verfügbar. Sprich den Satz manuell nach.');session.speakScore=60;daily().speak=true;save();renderStage();return}let r=new R();r.lang='de-DE';r.interimResults=false;r.maxAlternatives=1;let mic=document.getElementById('x12Mic');if(mic)mic.classList.add('listening');r.onresult=e=>{session.transcript=e.results[0][0].transcript;session.speakScore=similarity(session.transcript,session.lesson.phrase);daily().speak=true;save();renderStage()};r.onerror=()=>{session.transcript=t('صدا دریافت نشد؛ دوباره تلاش کن.','Kein verwertbares Signal. Versuche es erneut.');session.speakScore=40;renderStage()};r.onend=()=>{let m=document.getElementById('x12Mic');if(m)m.classList.remove('listening')};r.start()};
  window.x12LessonBack=()=>{if(session&&session.step>0){session.step--;renderStage()}};
  window.x12LessonNext=()=>{if(!session)return;if(session.step===3&&session.practice===null){toast(t('ابتدا یک پاسخ انتخاب کن','Wähle zuerst eine Antwort'));return}if(session.step<6){if(session.step===5){daily().review=true}session.step++;renderStage();return}finishSession()};
  function finishSession(){let s=session,score=scoreSession(),x=x12(),old=mastery(s.skill);x.mastery[s.skill]=Math.round(old*.65+score*.35);x.sessions.push({id:s.id,date:todayKey(),level:s.level,skill:s.skill,score,minutes:Math.max(8,Math.round((Date.now()-s.startedAt)/60000)),speakScore:s.speakScore,completedAt:new Date().toISOString()});x.lastLesson=s.id;daily().lesson=true;if(s.speakScore>0)daily().speak=true;daily().review=true;state.studySessions=Array.isArray(state.studySessions)?state.studySessions:[];state.studySessions.push({id:s.id,date:todayKey(),startedAt:new Date(s.startedAt).toISOString(),endedAt:new Date().toISOString(),minutes:Math.max(8,Math.round((Date.now()-s.startedAt)/60000)),module:s.skill,source:'X12 Lesson Engine'});save();let el=document.createElement('div');el.className='x12-toast-success';el.textContent=t('جلسه با موفقیت ثبت شد','Sitzung erfolgreich gespeichert');document.body.appendChild(el);setTimeout(()=>el.remove(),1800);closeLesson();renderAll()}

  // Version-aware shell cleanup.
  const oldSettings=window.openSettings;
  if(typeof oldSettings==='function')window.openSettings=function(){oldSettings();let sheet=document.getElementById('settingsSheet');if(sheet){sheet.querySelectorAll('.version').forEach(n=>n.remove());sheet.insertAdjacentHTML('beforeend',`<div class="small muted" style="text-align:center;margin-top:14px">DeutschWeg X12 · ${APP_VERSION}</div>`)}};

  // Start X12 mode immediately after the legacy app has initialized.
  state.ui=state.ui||{};
  if(!state.ui.x12Mode)state.ui.x12Mode='journey';
  save();
  renderAll();
})();
