const tr=(fa,de)=>document.documentElement.lang==='de'?de:fa;
export function createCloudCenter(){function open(){let c=window.DEUTSCHWEG_CLOUD||{},ready=Boolean(c.url&&c.anonKey),m=document.getElementById('dwxCloudModal')||Object.assign(document.createElement('div'),{id:'dwxCloudModal',className:'dwx1-modal'});if(!m.parentNode)document.body.appendChild(m);m.innerHTML=`<div class="dwx1-sheet"><button class="iconbtn close" id="dwxCloudClose">×</button><span class="kicker">Cloud & Multi-device</span><h2>${tr('همگام‌سازی اختیاری','Optionaler Cloud-Sync')}</h2><div class="card"><b>${ready?'✓ '+tr('پیکربندی Cloud موجود است','Cloud ist konfiguriert'):tr('حالت Local-First فعال است','Local-First aktiv')}</b><p class="muted">${tr('کاربر بدون حساب هم می‌تواند از برنامه استفاده کند. Cloud فقط برای بازیابی و همگام‌سازی بین دستگاه‌هاست.','Die App funktioniert ohne Konto. Cloud dient nur Wiederherstellung und geräteübergreifender Synchronisierung.')}</p></div><div class="notice">${tr('service_role یا کلید خصوصی را هرگز در config.js قرار ندهید.','Niemals service_role oder private Schlüssel in config.js eintragen.')}</div></div>`;m.classList.add('show');m.querySelector('#dwxCloudClose').onclick=()=>m.classList.remove('show')}window.openCloudCenterX=open;return{open}}
