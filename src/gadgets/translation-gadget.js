const tr=(fa,de)=>document.documentElement.lang==='de'?de:fa;
export function createTranslationGadget(){function open(){let m=document.getElementById('dwxTranslateModal')||Object.assign(document.createElement('div'),{id:'dwxTranslateModal',className:'dwx1-modal'});if(!m.parentNode)document.body.appendChild(m);m.innerHTML=`<div class="dwx1-sheet"><button class="iconbtn close" id="dwxTrClose">×</button><span class="kicker">Translation · Übersetzung</span><h2>${tr('ترجمه امن و اختیاری','Sichere optionale Übersetzung')}</h2><textarea id="dwxTrText" style="min-height:150px" placeholder="Text…"></textarea><div class="actions"><button class="primary" id="dwxTrDeepL">DeepL Web ↗</button><button class="ghost" id="dwxTrCopy">${tr('کپی متن','Text kopieren')}</button></div><div class="notice">${tr('نسخه فعلی کلید API را داخل مرورگر نگه نمی‌دارد. برای ترجمه API مستقیم باید Backend/Edge Function امن تنظیم شود.','Die aktuelle Version speichert keinen API-Schlüssel im Browser. Für API-Übersetzung ist ein sicherer Backend-/Edge-Proxy nötig.')}</div></div>`;m.classList.add('show');m.querySelector('#dwxTrClose').onclick=()=>m.classList.remove('show');m.querySelector('#dwxTrDeepL').onclick=()=>window.open('https://www.deepl.com/translator','_blank','noopener,noreferrer');m.querySelector('#dwxTrCopy').onclick=()=>navigator.clipboard?.writeText(m.querySelector('#dwxTrText').value)}window.openTranslationGadget=open;return{open}}
