export function createAdaptiveEngine(){function analyze(){let s=window.DeutschWegLegacyBridge?.getSnapshot?.()||{};let sessions=s.studySessions||[];let totals={};for(const x of sessions){let k=x.module||'Andere';totals[k]=(totals[k]||0)+Number(x.minutes||0)}let skills=['Hören','Sprechen','Lesen','Schreiben','Wortschatz','Grammatik'];let ranked=skills.map(k=>[k,totals[k]||0]).sort((a,b)=>a[1]-b[1]);return{weak:ranked[0]?.[0]||'Sprechen',strong:ranked[ranked.length-1]?.[0]||'Wortschatz',totals,recommendation:`${ranked[0]?.[0]||'Sprechen'}: 15–25 min`}}function apply(){let a=analyze();window.DeutschWegLegacyBridge?.setAdaptiveHint?.(a);return a}return{analyze,apply}}
